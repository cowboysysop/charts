apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubebox.fullname" . }}-test-connection"
  labels:
    {{- include "kubebox.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubebox.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
